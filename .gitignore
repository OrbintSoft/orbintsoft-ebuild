# Generated metadata cache — regenerated by `egencache` / CI, never hand-committed.
/metadata/md5-cache/
/metadata/pkg_desc_index

# Claude / assistant scratch & working-memory files (see CLAUDE.md rules 7–9).
# These may contain logs, errors or personal data and must never be committed.
*-steps.md
*.scratch
*.log
/.scratch/
/scratch/

# Editor / OS noise
*.swp
*~
.DS_Store
